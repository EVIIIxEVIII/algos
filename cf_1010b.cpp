#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;
    while (t--) {
        int x, n, m;
        cin >> x >> n >> m;


        if (n + m <= 32) {

        } else {
            if (n > 32) {
                cout << 0 << ' ' << 0 << '\n';
            } else {

            }
        }


    }
    return 0;
}
