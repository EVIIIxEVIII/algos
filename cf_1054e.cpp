#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;
    while (t--) {
        int k,l,r,n;
        cin >>k>>l>>r>>n;

        vector<int> a(n);
        for (int i = 0; i < n; ++i) {

        }

    }
    return 0;
}
