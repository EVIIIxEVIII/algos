#include <bits/stdc++.h>
using namespace std;

void solve();

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;
    while (t--) solve();

    return 0;
}

void solve() {
    int n, k;
    cin >> n >> k;

      ;
     ;;;
    ;;;;;
   ;;;;;;;
  ;;;;;;;;;
 ;;;;;;;;;;;
;;;;;;;;;;;;;


    // the number of odd number will be (n + 1) / 2,
    // the number of numbers obtained through 2 is
    // odd numbers can be skipped, because they don't add any cards
    //

}
